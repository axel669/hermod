region: us-west-1
profile: default
prodTag: "live"

functions:
  core:
    dir: service/hermod-core
  rtc-send:
    dir: service/rtc-send
  rtc-overlay-load:
    dir: service/hermod_rtc-overlay-settings

deployment:
  - "func:core"
  - "func:rtc-send"
